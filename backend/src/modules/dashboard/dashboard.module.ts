/**
 * 儿童仪表盘模块（占位）
 *
 * 待 IDE 实现：
 *   - GET /api/v1/dashboard  聚合：总星星数 + 进行中任务列表 + 许愿池进度
 *   - 可引入内存缓存优化首页秒开
 */
import { Module } from '@nestjs/common';

@Module({})
export class DashboardModule {}
